# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

verify-shots/
shots/

# Local state that must never be published
.wrangler/
output/
__pycache__/
.claude/settings.local.json

# Per-fork image generator settings; vstreat.config.example.json is committed
vstreat.config.json

# Per-world generation intermediates: aerial, blockouts, raw panoramas.
# The traced town.geojson beside them is committed, because it is the source
# every later stage derives from.
worlds/*/raw/

# A recorded exhibit loop
*.webm
